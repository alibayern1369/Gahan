# راهنمای کامل نصب و راه‌اندازی «گاهان»

**سامانه هوشمند حضور و غیاب گاهان**

این راهنما برای کسی نوشته شده که متخصص نرم‌افزار نیست؛ فقط قدم‌به‌قدم جلو بروید. اگر به هر بخش دقت کنید، در حدود یک ساعت سامانهٔ شما آمادهٔ کار خواهد بود.

---

## فهرست

1. [پیش‌نیازها](#۱-پیشنیازها)
2. [ساخت پروژه در Supabase](#۲-ساخت-پروژه-در-supabase)
3. [چه اطلاعاتی از Supabase بردارم؟](#۳-چه-اطلاعاتی-از-supabase-بردارم)
4. [Environment Variables چیست؟](#۴-environment-variables-چیست)
5. [هر متغیر را از کجا پیدا کنم؟](#۵-هر-متغیر-را-از-کجا-پیدا-کنم)
6. [کدام متغیرها محرمانه هستند؟](#۶-کدام-متغیرها-محرمانه-هستند)
7. [اجرای Migrationها (ساخت جدول‌ها)](#۷-اجرای-migrationها)
8. [RLS چگونه فعال می‌شود؟](#۸-rls-چگونه-فعال-میشود)
9. [Storage Bucket ها چگونه ساخته می‌شوند؟](#۹-storage-bucket-ها-چگونه-ساخته-میشوند)
10. [حذف خودکار عکس‌های بیش از ۳۰ روز](#۱۰-حذف-خودکار-عکسهای-بیش-از-۳۰-روز)
11. [ساخت اولین مدیر (Admin)](#۱۱-ساخت-اولین-مدیر)
12. [افزودن کارمند جدید](#۱۲-افزودن-کارمند-جدید)
13. [تنظیم محل شرکت و مختصات](#۱۳-تنظیم-محل-شرکت-و-مختصات)
14. [تعیین شعاع مجاز](#۱۴-تعیین-شعاع-مجاز)
15. [تعیین ساعت کاری](#۱۵-تعیین-ساعت-کاری)
16. [نام سازمان و برندینگ گاهان](#۱۶-نام-سازمان-و-برندینگ-گاهان)
17. [بارگذاری لوگوها و فاویکن](#۱۷-بارگذاری-لوگوها-و-فاویکن)
18. [محدودیت فرمت و حجم فایل‌ها](#۱۸-محدودیت-فرمت-و-حجم-فایلها)
19. [قرار دادن پروژه در GitHub](#۱۹-قرار-دادن-پروژه-در-github)
20. [اتصال پروژه به Vercel](#۲۰-اتصال-پروژه-به-vercel)
21. [محل وارد کردن Environment Variables در Vercel](#۲۱-محل-وارد-کردن-env-در-vercel)
22. [Deploy کردن](#۲۲-deploy-کردن)
23. [اتصال دامنه شخصی (اختیاری)](#۲۳-اتصال-دامنه-شخصی)
24. [تست‌های بعد از Deploy](#۲۴-تستهای-بعد-از-deploy)
25. [اگر دوربین کار نکرد](#۲۵-اگر-دوربین-کار-نکرد)
26. [اگر موقعیت مکانی کار نکرد](#۲۶-اگر-موقعیت-مکانی-کار-نکرد)
27. [تنظیمات iPhone](#۲۷-تنظیمات-iphone)
28. [تنظیمات Android](#۲۸-تنظیمات-android)
29. [گرفتن گزارش](#۲۹-گرفتن-گزارش)
30. [پاک شدن عکس‌های قدیمی](#۳۰-پاک-شدن-عکسهای-قدیمی)
31. [Backup و Export داده‌ها](#۳۱-backup-و-export-دادهها)
32. [Update کردن برنامه در آینده](#۳۲-update-کردن-برنامه-در-آینده)
33. [رایج‌ترین خطاها و راه حل](#۳۳-رایجترین-خطاها-و-راه-حل)

---

## ۱. پیش‌نیازها

- یک حساب رایگان **GitHub**: [github.com](https://github.com)
- یک حساب رایگان **Supabase**: [supabase.com](https://supabase.com)
- یک حساب رایگان **Vercel**: [vercel.com](https://vercel.com) — می‌توانید با همان حساب GitHub ثبت‌نام کنید
- (فقط برای اجرای محلی/توسعه) نصب **Node.js** نسخهٔ ۲۰ یا بالاتر از [nodejs.org](https://nodejs.org)

هزینهٔ این سه سرویس برای سازمان زیر ۵۰ نفر صفر است.

---

## ۲. ساخت پروژه در Supabase

Supabase همان جایی است که **دیتابیس، حساب‌های کاربری و عکس‌ها** نگهداری می‌شوند.

1. وارد [supabase.com](https://supabase.com) شوید و با ایمیل خود ثبت‌نام کنید.
2. دکمهٔ **New project** را بزنید.
3. یک نام انتخاب کنید (مثلاً `gahan`)، برای دیتابیس یک گذرواژه بسازید و آن را **جایی یادداشت کنید**.
4. Region را نزدیک‌ترین منطقه انتخاب کنید (مثلاً `Frankfurt` یا `Bahrain`).
5. چند دقیقه صبر کنید تا پروژه ساخته شود.

---

## ۳. چه اطلاعاتی از Supabase بردارم

بعد از ساخت پروژه، از منوی چرخ‌دندهٔ بالا سمت چپ، وارد **Project Settings → API** شوید. سه مقدار اینجا لازم دارید:

| نام | توضیح | نمونه |
|---|---|---|
| **Project URL** | آدرس پروژه شما | `https://abcdefgh.supabase.co` |
| **anon public key** | کلید عمومی (امن برای مرورگر) | رشتهٔ طولانی `eyJ...` |
| **service_role key** | کلید مدیریتی (**خیلی محرمانه**) | رشتهٔ طولانی دیگر |

این سه مقدار را فعلاً در یک فایل متنی امن ذخیره کنید.

---

## ۴. Environment Variables چیست

Environment Variables یعنی «متغیرهای محیطی»: مقادیری مثل آدرس دیتابیس و کلیدها که **داخل کد برنامه نوشته نمی‌شوند** تا لو نروند. برنامه هنگام اجرا آن‌ها را از تنظیمات سرور می‌خواند. دو دسته دارند:

- `NEXT_PUBLIC_...` → برای مرورگر اشکالی ندارد که دیده شوند (کلید عمومی).
- بدون پیشوند → **فقط روی سرور** می‌مانند و هرگز نباید لو بروند (کلید مدیریتی).

در ریپوی پروژه یک فایل نمونه به اسم `.env.example` هست که نام همهٔ متغیرها را نشان می‌دهد.

---

## ۵. هر متغیر را از کجا پیدا کنم

همه از همان صفحهٔ **Project Settings → API** در Supabase:

| متغیر برنامه | معادل در Supabase |
|---|---|
| `NEXT_PUBLIC_SUPABASE_URL` | Project URL |
| `NEXT_PUBLIC_SUPABASE_ANON_KEY` | anon / public |
| `SUPABASE_SERVICE_ROLE_KEY` | service_role / secret |
| `CRON_SECRET` | خودتان بسازید: یک رشتهٔ تصادفی طولانی، مثلاً خروجی [randomkeygen.com](https://randomkeygen.com) |

---

## ۶. کدام متغیرها محرمانه هستند

- ✅ امن: `NEXT_PUBLIC_SUPABASE_URL` و `NEXT_PUBLIC_SUPABASE_ANON_KEY` — این دو داخل مرورگر کاربران هم قابل مشاهده‌اند و طراحی سامانه طوری است که مشکلی ایجاد نمی‌کند.
- ⛔ **محرمانه:** `SUPABASE_SERVICE_ROLE_KEY` — با این کلید هرکسی به همهٔ داده‌ها دسترسی پیدا می‌کند! فقط در Vercel (یا فایل `.env.local` محلی که کامیت نمی‌شود) واردش کنید.
- ⛔ **محرمانه:** `CRON_SECRET` — فقط جلوی اجرای غیرمجاز پاک‌سازی عکس‌ها را می‌گیرد.

هرگز این دو را در ایمیل، گروه تلگرام یا داخل کد ارسال نکنید.

---

## ۷. اجرای Migrationها

Migrationها فایل‌هایی هستند که جدول‌های دیتابیس را می‌سازند. نیازی نیست چیزی دستی بسازید!

1. در پروژهٔ Supabase از منوی چپ روی **SQL Editor** کلیک کنید.
2. از پوشهٔ `supabase/migrations` پروژهٔ گاهان، فایل‌ها را **به ترتیب شماره** باز کنید:
   - `0001_schema.sql`
   - `0002_functions.sql`
   - `0003_rls.sql`
   - `0004_storage.sql`
   - `0005_cleanup.sql`
3. محتوای هر فایل را کامل کپی، در SQL Editor پیست و دکمهٔ **Run** را بزنید.
4. بعد از هر Run باید پیام موفقیت ببینید. اگر فایل قبلاً اجرا شده باشد، دوباره اجرایش بی‌ضرر است (idempotent).

بعد از اتمام، در منوی **Table Editor** باید جدول‌هایی مثل `profiles`، `attendance_sessions` و `app_settings` را ببینید.

> روش حرفه‌ای‌تر: اگر Supabase CLI نصب دارید، کافی است دستور `supabase db push` را بزنید.

---

## ۸. RLS چگونه فعال می‌شود

**RLS (Row Level Security)** یعنی هر کاربر فقط رکوردهای مجاز خودش را ببیند؛ حتی اگر کسی کلاینت برنامه را دستکاری کند.

خبر خوب: **migration شمارهٔ `0003_rls.sql` همهٔ RLS را خودکار فعال می‌کند.** کاری لازم نیست انجام دهید. فقط بدانید:

- کارمند فقط سوابق خودش را می‌بیند.
- عکس سلفی هر نفر فقط در پوشهٔ اختصاصی خودش ذخیره و فقط برای مدیر نمایش داده می‌شود.
- تغییر نقش‌ها و اصلاح رکوردها فقط برای نقش admin ممکن است و همه در لاگ ممیزی ثبت می‌شود.

---

## ۹. Storage Bucket ها چگونه ساخته می‌شوند

Bucket یعنی «فضای ذخیره فایل». دو bucket لازم داریم که migration شمارهٔ `0004_storage.sql` هر دو را می‌سازد:

| Bucket | دسترسی | کاربرد |
|---|---|---|
| `selfies` | **خصوصی** — فقط صاحب عکس و مدیر | عکس‌های سلفی حضور و غیاب |
| `branding` | عمومی (فقط خواندن) | لوگو و فاویکن |

بعد از اجرای migration، در منوی **Storage** هر دو را خواهید دید. اگر به هر دلیل bucket‌ها ساخته نشدند، محتوای همان فایل SQL را در SQL Editor اجرا کنید.

⚠️ مهم: bucket سلفی را **عمومی نکنید**. حریم خصوصی افراد به آن بستگی دارد.

---

## ۱۰. حذف خودکار عکسهای بیش از ۳۰ روز

طبق سیاست حفظ حریم خصوصی، عکس سلفی بعد از حدود **۳۰ روز** باید پاک شود ولی **رکورد حضور باقی می‌ماند** (برای گزارش‌ها).

این پاک‌سازی به‌صورت خودکار انجام می‌شود و **با Vercel Cron از قبل تنظیم شده است**؛ فقط باید متغیر `CRON_SECRET` را در Vercel ست کرده باشید (بخش ۲۱). همین!

جزئیات فنی:

- فایل‌ها از Storage واقعاً حذف می‌شوند و در رکورد، ستون‌های `checkin_photo_deleted_at` / `checkout_photo_deleted_at` پر می‌شوند.
- مدت نگهداری در **تنظیمات ▸ عمومی ▸ مدت نگهداری عکس‌های سلفی** قابل تغییر است (پیش‌فرض ۳۰).
- دکمهٔ **«اجراهای دستی پاک‌سازی»** هم در همان صفحه هست.
- آپلودهای ناقص (بدون ثبت نهایی) بعد از ۲۴ ساعت هم پاک می‌شوند.

**روش جایگزین (اختیاری):** اگر می‌خواهید پاک‌سازی داخل خود Supabase زمان‌بندی شود:
1. در داشبورد Supabase دو افزونهٔ **pg_cron** و **pg_net** را فعال کنید.
2. دستور `npx supabase functions deploy cleanup-selfies` را اجرا کنید و سپس secret بدهید: `npx supabase secrets set CLEANUP_SECRET=<یک رشته تصادفی>`
3. فایل `supabase/optional/cron-cleanup.sql` را باز کنید، مقادیر placeholder را پر کنید و در SQL Editor اجرا کنید.

---

## ۱۱. ساخت اولین مدیر

ساده‌ترین راه، اسکریپت آماده است (نیاز به Node.js روی کامپیوتر شما دارد):

1. فایل `.env.local` کنار پروژه بسازید و این‌ها را بگذارید:
   ```
   NEXT_PUBLIC_SUPABASE_URL=https://xxxx.supabase.co
   SUPABASE_SERVICE_ROLE_KEY=eyJ...
   SEED_ADMIN_EMAIL=modir@sherkat.com
   SEED_ADMIN_PASSWORD=YekRamzGhavi123!
   ALLOW_DEMO_SEED=true
   ```
2. در ترمینال، داخل پوشهٔ پروژه بنویسید: `npm install` و بعد `npm run db:seed`

اسکریپت یک مدیر + محل کاری + برنامهٔ کاری + چند کارمند نمونه با دادهٔ تمرینی می‌سازد. حالا می‌توانید با ایمیل و رمزی که دادید وارد شوید.

⚠️ **بعد از تست واقعی، کارکنان نمونه را از پنل مدیریت غیرفعال کنید** و `ALLOW_DEMO_SEED` را حذف کنید. اگر نمی‌خواهید دادهٔ نمونه ساخته شود، بخش کارمندان اسکریپت را نادیده بگیرید یا فقط مدیر بسازید.

> راه دوم (بدون اسکریپت): در Supabase ▸ Authentication ▸ Users ▸ Add user یک کاربر بسازید، سپس در Table Editor ▸ profiles یک ردیف با `user_id = شناسهٔ همان کاربر` و `role = admin` اضافه کنید.

---

## ۱۲. افزودن کارمند جدید

1. با حساب مدیر وارد شوید → منوی **کارمندان** → دکمهٔ **«افزودن کارمند»**.
2. نام، ایمیل و بقیهٔ مشخصات را پر کنید. محل کاری و برنامهٔ کاری را انتخاب کنید.
3. سامانه یک **گذرواژهٔ موقت** می‌سازد که فقط یک بار نمایش داده می‌شود — آن را کپی و برای کارمند بفرستید.
4. کارمند با ایمیل و گذرواژهٔ موقت وارد می‌شود و از صفحهٔ «حساب» می‌تواند گذرواژه را عوض کند.

غیرفعال کردن: در صفحهٔ کارمند تیک «حساب فعال باشد» را بردارید (سابقهٔ حضور حفظ می‌شود). برای بازگشت، دوباره فعالش کنید.

---

## ۱۳. تنظیم محل شرکت و مختصات

1. منوی **موقعیت‌های کاری** → **محل کاری جدید**.
2. نام (مثلاً «دفتر مرکزی») و شعاع مجاز را وارد کنید.
3. عرض/طول جغرافیایی: در **Google Maps** روی محل شرکت **راست‌کلیک** کنید و اولین گزینه (مختصات) را کپی کنید. عدد اول Latitude و عدد دوم Longitude است. مثال تهران: `35.7448, 51.3753`.

می‌توانید چند محل (دفتر، انبار، فروشگاه…) بسازید و به هر کارمند یکی را نسبت دهید.

---

## ۱۴. تعیین شعاع مجاز

شعاع مجاز یعنی چند متر حول محل کار، ثبت حضور قبول است:

- در همان فرم محل کاری، فیلد **«شعاع مجاز (متر)»** را تنظیم کنید (مثلاً ۱۵۰ متر).
- یک مقدار پیش‌فرض هم در **تنظیمات ▸ عمومی** وجود دارد که برای محل‌های جدید استفاده می‌شود.
- توصیه: برای دقت GPS گوشی‌ها، کمتر از ۱۰۰ متر معمولاً سخت‌گیرانه است؛ ۱۵۰ تا ۳۰۰ متر تجربهٔ بهتری می‌دهد.
- **حداکثر دقت قابل‌قبول GPS** (پیش‌فرض ۱۰۰ متر) هم در تنظیمات است: اگر موقعیتِ گزارش‌شدهٔ گوشی دقیق‌تر از این نباشد، ثبت رد می‌شود و به کارمند پیام راهنما نمایش داده می‌شود.

---

## ۱۵. تعیین ساعت کاری

منوی **برنامه کاری** → **برنامهٔ جدید**:

- نام برنامه (مثلاً «شیفت اداری صبح»)
- ساعت شروع و پایان (مثلاً ۰۹:۰۰ تا ۱۷:۰۰)
- **گرِیس** = چند دقیقه تأخیر قابل چشم‌پوشی است (مثلاً ۱۰ دقیقه)
- روزهای کاری را تیک بزنید (شنبه تا چهارشنبه و …)

سپس در صفحهٔ هر کارمند، برنامهٔ او را انتخاب کنید. کارمند بدون برنامه از هفتهٔ کاری پیش‌فرض تنظیمات پیروی می‌کند. تأخیر، تعجیل خروج و اضافه‌کار بر همین اساس محاسبه می‌شود.

---

## ۱۶. نام سازمان و برندینگ گاهان

در **تنظیمات ▸ عمومی**:

- **نام سازمان** (مثلاً «شرکت نمونه») — در ورود و داشبورد نمایش داده می‌شود.
- **نام برنامه** — پیش‌فرض «گاهان» است.
- **شعار** — پیش‌فرض «سامانه هوشمند حضور و غیاب».
- **منطقه زمانی** — پیش‌فرض `Asia/Tehran`. همهٔ تاریخ‌ها و محاسبات «امروز» بر همین اساس است.

---

## ۱۷. بارگذاری لوگوها و فاویکن

**تنظیمات ▸ برندینگ و لوگوها** چهار جایگاه دارد:

1. **لوگوی حالت روشن** — وقتی تم روشن است نمایش داده می‌شود.
2. **لوگوی حالت تاریک** — وقتی تم تاریک است. اگر بارگذاری نشود، از لوگوی روشن استفاده می‌شود.
3. **فاویکن** — آیکون کنار عنوان مرورگر.
4. **آیکون PWA** — آیکونی که هنگام «Add to Home Screen» روی گوشی نصب می‌شود.

روی «بارگذاری/جایگزینی» بزنید، فایل را انتخاب کنید و تمام؛ در همه‌جا (ورود، هدر، manifest) به‌روز می‌شود. با «حذف» به وردمارک پیش‌فرض **گاهان** برمی‌گردید.

نکته: بهترین نتیجهٔ لوگو، فایل PNG با پس‌زمینهٔ شفاف است. برای حالت تاریک معمولاً نسخهٔ سفید/روشن رنگ لوگو مناسب است.

---

## ۱۸. محدودیت فرمت و حجم فایلها

| جایگاه | فرمت‌های مجاز | حداکثر حجم | توصیه |
|---|---|---|---|
| لوگو روشن/تاریک | PNG, JPG, WebP, SVG | ۵۱۲ KB | حداقل ۲۵۶×۶۴ پیکسل |
| فاویکن | PNG, SVG | ۲۵۶ KB | مربعی ۶۴×۶۴ یا بزرگتر |
| آیکون PWA | **فقط PNG** | ۵۱۲ KB | **۵۱۲×۵۱۲** مربع کامل |

- فایل SVG قبل از ذخیره به‌صورت خودکار پاک‌سازی امنیتی می‌شود.
- آیکون PWA حتماً PNG باشد تا iOS و Android درست نمایشش بدهند.

---

## ۱۹. قرار دادن پروژه در GitHub

1. در GitHub یک **New repository** بسازید (نام دلخواه مثل `gahan`، Private بهتر است).
2. در کامپیوتر، داخل پوشهٔ پروژه:
   ```
   git init
   git add .
   git commit -m "Gahan attendance app"
   git branch -M main
   git remote add origin https://github.com/USERNAME/gahan.git
   git push -u origin main
   ```

⚠️ فایل `.gitignore` پروژه طوری تنظیم شده که فایل‌های `.env*` هرگز آپلود نشوند — پس کلیدهای شما امن می‌مانند. با این حال قبل از Push یک بار `git status` را چک کنید.

---

## ۲۰. اتصال پروژه به Vercel

1. وارد [vercel.com](https://vercel.com) شوید و **Add New → Project** بزنید.
2. مخزن GitHub که ساخته‌اید را **Import** کنید.
3. قبل از Deploy، پایین صفحه بخش **Environment Variables** را باز کنید (بخش بعدی).
4. **Deploy** را بزنید.

Vercel خودش تشخیص می‌دهد Next.js است و تنظیمات پیش‌فرض کافی است.

---

## ۲۱. محل وارد کردن Env در Vercel

در همان صفحهٔ Import (یا بعداً از مسیر Project → Settings → Environment Variables) برای هر متغیر Name و Value را وارد کنید:

```
NEXT_PUBLIC_SUPABASE_URL     = https://xxxx.supabase.co
NEXT_PUBLIC_SUPABASE_ANON_KEY = eyJ...
SUPABASE_SERVICE_ROLE_KEY    = eyJ...   (محرمانه)
CRON_SECRET                  = رشته تصادفی طولانی
```

اگر بعداً متغیری اضافه کردید، از تب Settings → Environment Variables اضافه کنید و یک **Redeploy** بزنید.

---

## ۲۲. Deploy کردن

- اولین Deploy با زدن دکمه در مرحلهٔ قبل انجام شد. ۲–۳ دقیقه صبر کنید.
- آخرش یک آدرس مثل `https://gahan.vercel.app` می‌گیرید — همان آدرس اصلی برنامهٔ شماست.
- از این به بعد هر `git push` به GitHub = انتشار خودکار نسخهٔ جدید.

---

## ۲۳. اتصال دامنه شخصی

اختیاری است:

1. یک دامنه از سایت‌های فروش دامنه بخرید (مثل `sherkat.ir`).
2. در Vercel: Project → Settings → Domains → **Add** و دامنه را وارد کنید.
3. Vercel چند رکورد DNS (معمولاً A و CNAME) به شما می‌دهد؛ در پنل دامنهٔ خودتان واردشان کنید.
4. چند دقیقه تا چند ساعت صبر کنید؛ SSL خودکار فعال می‌شود.

نکته: دوربین و موقعیت مکانی **فقط روی HTTPS** کار می‌کنند — هم آدرس vercel.app و هم دامنهٔ شخصی شما HTTPS دارند.

---

## ۲۴. تستهای بعد از Deploy

این چک‌لیست را با یک گوشی واقعی انجام دهید:

- [ ] ورود مدیر و کارمند با موفقیت انجام می‌شود.
- [ ] در فاصلهٔ مجاز، «ثبت ورود» کار می‌کند: موقعیت گرفته می‌شود → دوربین باز می‌شود → عکس → پیش‌نمایش → ثبت → صفحهٔ موفقیت سبز.
- [ ] «ثبت خروج» بعد از ورود کار می‌کند و مدت کارکرد نمایش داده می‌شود.
- [ ] دوباره زدن «ثبت ورود» وقتی داخل شیفت هستید، خطای مهربانانه می‌دهد (رکورد تکراری ساخته نمی‌شود).
- [ ] خارج از شعاع، پیام «شما خارج از محدوده مجاز قرار دارید» می‌آید و رکوردی ثبت نمی‌شود.
- [ ] در پنل مدیر، «حضور امروز» رکورد جدید را با سلفی نشان می‌دهد؛ جزئیات رکورد، فاصله و نقشه را می‌بینید.
- [ ] گزارش‌ها فیلتر و خروجی CSV/Excel می‌دهند و فارسی درست باز می‌شود.
- [ ] تم روشن/تاریک/سیستم از دکمهٔ ماه/خورشید عوض می‌شود.
- [ ] لوگوها و فاویکن سفارشی نمایش داده می‌شوند.
- [ ] در iPhone: Share → Add to Home Screen → آیکون و نام گاهان درست است.

---

## ۲۵. اگر دوربین کار نکرد

به ترتیب بررسی کنید:

1. **HTTPS** هستید؟ (`https://…`) — دوربین روی http کار نمی‌کند.
2. اجازهٔ دوربین: آیکون قفل کنار آدرس → Permissions → Camera → Allow.
3. صفحه را کاملاً ببندید و دوباره باز کنید.
4. برنامهٔ دیگری (تماس ویدیویی) دوربین را اشغال نکرده باشد.
5. اگر باز هم نشد: دکمهٔ «گرفتن عکس با دوربین گوشی» به‌طور خودکار ظاهر می‌شود که از اپ دوربین خودِ گوشی استفاده می‌کند — این مسیر همیشه کار می‌کند.

---

## ۲۶. اگر موقعیت مکانی کار نکرد

1. **GPS گوشی (Location)** روشن باشد.
2. اجازهٔ Location برای مرورگر: آیکون قفل → Location → Allow. در iOS گزینهٔ **Precise** را فعال کنید.
3. داخل ساختمان‌های فلزی/زیرزمین سیگنال ضعیف است — نزدیک پنجره بروید.
4. اگر پیام «دقت موقعیت کافی نیست» آمد: چند ثانیه صبر کنید و دوباره بزنید؛ سامانه بهترین دقت بین چند تلاش را می‌گیرد.
5. اگر هنوز مشکل داشتید، در تنظیمات مدیریت مقدار «حداکثر دقت قابل‌قبول GPS» را موقتاً بیشتر کنید (مثلاً ۲۰۰).

---

## ۲۷. تنظیمات iPhone

- **Safari → Settings → Camera / Location** یا مستقیماً: Settings → Safari → Camera → Allow؛ Location → While Using.
- برای دقت بالا: Settings → Privacy & Security → Location Services → مرورگر → Precise Location روشن.
- نصب روی صفحهٔ اصلی: در Safari دکمهٔ Share → **Add to Home Screen**.

---

## ۲۸. تنظیمات Android

- Chrome → آیکون قفل کنار سایت → Permissions → Camera/Location → Allow.
- Settings → Apps → Chrome → Permissions → Location → Allow all the time (برای دقت بهتر).
- نصب: منوی سه‌نقطه → **Add to Home screen**.

---

## ۲۹. گرفتن گزارش

منوی **گزارش‌ها**:

- بازهٔ سریع: امروز / این هفته / این ماه / ماه گذشته / بازه دلخواه (تاریخ شمسی).
- فیلتر کارمند، محل کار و وضعیت.
- کارت‌های خلاصه: انتظاری، حاضر، غایب، تأخیر، کارکرد، اضافه‌کار، خروج زودهنگام، خروج ثبت‌نشده.
- جدول «به تفکیک کارمند» و لیست تفصیلی رکوردها با امکان باز کردن جزئیات.
- خروجی: دکمه‌های **CSV** و **اکسل** بالای صفحه — فایل دانلودی با هدرهای فارسی و سازگار با Excel.

---

## ۳۰. پاک شدن عکسهای قدیمی

خودکار است! هر شب یک job عکس‌هایی را که از عمرشان گذشته (پیش‌فرض ۳۰ روز) از Storage حذف می‌کند و در رکورد فقط علامت «عکس حذف‌شده» می‌گذارد. تاریخچهٔ حضور، زمان‌ها و مختصات همیشه می‌مانند. برای اجرای فوری، دکمهٔ «اجراهای دستی پاک‌سازی» در تنظیمات است. (بخش ۱۰ را ببینید.)

---

## ۳۱. Backup و Export دادهها

- **دیتابیس کامل:** داشبورد Supabase → Database → Backups (در پلن رایگان، snapshot روزانهٔ ۷ روزه موجود است). برای دانلود دستی: Database → Backups → Download یا ابزار `pg_dump`.
- **گزارش‌ها:** از خود برنامه خروجی CSV/Excel بگیرید — ساده‌ترین Backup قابل‌خواندن.
- **عکس‌ها:** به‌طور عمد کوتاه‌مدت نگهداری می‌شوند؛ اگر موردی لازم داشتید، قبل از ۳۰ روز از صفحهٔ جزئیات رکورد ذخیره‌اش کنید.

---

## ۳۲. Update کردن برنامه در آینده

1. نسخهٔ جدید کد را دریافت کنید (اگر خودتان تغییر داده‌اید، commit و push کنید).
2. اگر فایل‌های migration جدید در `supabase/migrations` اضافه شده بود، فقط آن‌ها را مثل بخش ۷ در SQL Editor اجرا کنید (فایل‌های قدیمی را اجرا نکنید).
3. Push به GitHub → Vercel خودش Deploy می‌کند.
4. اگر متغیر محیطی جدیدی در README آمده بود، در Vercel اضافه کنید.

نکتهٔ آرامش‌بخش: migrationهای این پروژه idempotent هستند؛ اجرای مجدد خرابی ایجاد نمی‌کند و داده‌های شما را پاک نمی‌کنند.

---

## ۳۳. رایجترین خطاها و راه حل

| خطا / علامت | علت احتمالی | راه حل |
|---|---|---|
| «متغیرهای NEXT_PUBLIC_SUPABASE_… تنظیم نشده‌اند» | env خالی است | بخش ۵ و ۲۱ |
| ورود می‌زند اما «پروفایل یافت نشد» | کاربر auth بدون ردیف در profiles | با seed بسازید یا طبق بخش ۱۱ راه دوم |
| «شما خارج از محدوده مجاز…» | GPS ضعیف یا واقعاً خارج | بخش ۲۶؛ شعاع را در بخش ۱۴ تنظیم کنید |
| «دقت موقعیت مکانی کافی نیست» | دقت GPS بدتر از حد مجاز | نزدیک پنجره؛ یا افزایش حد مجاز در تنظیمات |
| دوربین باز نمی‌شود | HTTP یا عدم اجازه | بخش ۲۵ (مسیر جایگزین خودکار هم دارید) |
| «حساب کاربری شما فعال نیست» | کارمند غیرفعال شده | پنل مدیر ▸ کارمندان ▸ فعال‌سازی |
| عکس در جزئیات رکورد نمی‌آید | عکس بعد از دورهٔ نگهداری حذف شده (طبیعی) یا لینک امضاشده منقضی — صفحه را Refresh کنید | — |
| خروجی CSV فارسی به‌هم‌ریخته | باز شدن با نرم‌افزار قدیمی | فایل UTF-8 BOM است؛ در Excel مدرن/LibreOffice درست باز می‌شود |
| خطای 401 در `/api/cron/cleanup` | CRON_SECRET ست نیست | بخش ۲۱ |
| Deploy شد ولی صفحات خطای 500 | env اشتباه یا migrations اجرا نشده | بخش ۷ و بازبینی متغیرها |

---

## پشتیبانی سریع از مفاهیم

- **RPC:** تابعی داخل دیتابیس که منطق اصلی (محاسبهٔ فاصله، ثبت رکورد) را امن اجرا می‌کند؛ کلاینت فقط نتیجه را می‌بیند.
- **Signed URL:** لینک موقت (چند دقیقه‌ای) برای دیدن عکس خصوصی؛ کسی بدون دسترسی، لینک ندارد.
- **Audit Log:** دفترچهٔ تغییرات مدیریتی — چه کسی، چه وقت، چه چیزی را عوض کرد.

**گاهان — سامانه هوشمند حضور و غیاب. موفق باشید! 🕐**
